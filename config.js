const config = {
  client: "mysql",
  connection: {
    host: "localhost",
    user: "developer",
    password: "193822979!@#",
    database: "messagingapp",
  },
};

module.exports = config;
